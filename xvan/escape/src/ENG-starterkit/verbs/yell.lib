
$VERB yell SYNONYM shout
 PROLOGUE
   if not(testflag(o_actor.f_alive)) then
     if trigger(o_actor.t_talk_to_dead) then
       disagree()
     endif
   endif

 DEFAULT
   printcr("Aaaargh.")

ENDVERB
