
$VERB unuse
 "unuse [o_subject]"
   if owns(o_player,o_subject) then
     print("\n> drop [o_subject].\n")
     if try(l_location, 0, "drop [o_subject]") then
       agree()
     endif
   endif

 DEFAULT
   agree() # do nothing

ENDVERB
